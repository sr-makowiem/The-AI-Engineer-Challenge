# AWS Configuration for Claude via Bedrock
AWS_REGION=us-east-1
# AWS credentials should be configured via AWS CLI or environment variables
# Run: aws configure sso --profile ai
# Then: export AWS_PROFILE=ai
